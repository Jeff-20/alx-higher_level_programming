-- creates table 'first_table' in the current database
-- in my MySQL server. The database name is passed as
-- an argument of the 'mysql' command
CREATE TABLE IF NOT EXISTS first_table (id INT, name VARCHAR(256));
