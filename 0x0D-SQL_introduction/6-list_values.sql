-- lists all rows of the table 'first_table'
-- from the database 'htbn_0c_0' in MySQL server.
-- All fields are to be printed and the database
-- is passed as the argument of the 'mysql' command
SELECT id, name FROM first_table;
